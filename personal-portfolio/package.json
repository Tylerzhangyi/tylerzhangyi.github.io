{
  "name": "personal-portfolio",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 8806",
    "dev:clean": "rm -rf .next node_modules/.cache && next dev -p 8806",
    "dev:turbo": "rm -rf .next && next dev --turbo -p 8806",
    "build": "next build",
    "build:clean": "rm -rf .next out node_modules/.cache && next build",
    "start": "next start -p 8806",
    "preview": "npx serve out -p 8806"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0",
    "gsap": "^3.15.0",
    "marked": "^17.0.0",
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
